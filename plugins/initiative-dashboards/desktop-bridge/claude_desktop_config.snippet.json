{
  "_comment": "Merge the 'mcpServers' block below into your existing claude_desktop_config.json. Replace ABSOLUTE_PATH_TO_REPO with the path where you checked out pga-marketplace. The INITIATIVE_DASHBOARDS_DIR env var is optional — defaults to ~/initiative-dashboards/ if omitted.",
  "mcpServers": {
    "initiative-dashboards": {
      "command": "python3",
      "args": [
        "ABSOLUTE_PATH_TO_REPO/plugins/initiative-dashboards/desktop-bridge/mcp_server.py"
      ],
      "env": {
        "INITIATIVE_DASHBOARDS_DIR": "/Users/YOUR_USER/initiative-dashboards"
      }
    }
  }
}
